{
  "name": "tts-engine-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "cap:setup": "npm install @capacitor/core @capacitor/cli @capacitor/android && npx cap init TTS Engine com.ttsengine.app --web-dir=dist && npx cap add android && node scripts/fix-java-version.js",
    "cap:sync": "npm run build && npx cap sync android && node scripts/fix-java-version.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "~5.6.2",
    "vercel": "^54.4.1",
    "vite": "^6.0.0"
  }
}
